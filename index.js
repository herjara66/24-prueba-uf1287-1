const express = require('express')
const app = express()
const port = 3000

function suma(sumando1, sumando2) {

  var sumando1 = 8
  var sumando2 = 9
  return resultado
}

app.get("/hola", (req, res) => { 
  var sumando1=()
  var sumando2=()

  suma(sumando1, sumando2)
  res.send("esta es el resultado:")
})
suma (sumando1+sumando2)

app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`)
}) 
